# U2_ASTEROID_OVERHAUL — Implementer Report

Implemented size enum + seeding, per-size radii/draw, ore chips + crack overlays, loot burst component, obstacle Velocity2 drift, rock-rock collision mask, knockback on projectile hit, combat→mining position sync with drag.

Ore readability uses UI resource icon chips on ferrite/lumen rocks (atlas regions already exist for all sizes). Full pixel regen of vein textures deferred as optional polish.
