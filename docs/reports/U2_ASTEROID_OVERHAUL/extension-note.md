# U2 Extension Note

- Baking ore veins into atlas PNGs can replace UI chip overlays without gameplay changes.
- Multi-frame crack animations can replace single `asteroids/break` tint overlays.
- Mass-based bounce (large vs small) can extend the current equal elastic exchange.
