# U3 Extension Note

- Future projectile art that faces up (like ships) should use `DrawRegionRotated` without the seeker π/2 cancel.
- Prefer a shared draw helper keyed by atlas forward-axis metadata if more +X projectiles are added.
- No persistence/schema impact.
